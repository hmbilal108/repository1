application programmable interface -> takes request form client and go to db and bring response 
COPY -> The COPY instruction lets us copy a file (or files) from the host system into the image.
COPY -> source destination
docker network create twotier -> if two individual containers want to talk to each other then we need a network 
seeding -> table creation at runtime 
docker-compose -> creates network on its own


 






